package com.duckgo.medtools.medicaltools.expandtabviewmenu.surgical.orthopedics

import com.duckgo.medtools.databinding.MedcaltoolMenuPageFragmentBinding
import com.duckgo.medtools.medicaltools.basefragment.Med_Tool_Menu_BaseFragment

class orthopedics_Menu : Med_Tool_Menu_BaseFragment<MedcaltoolMenuPageFragmentBinding>() {

    override fun getFragmentViewBinding() = MedcaltoolMenuPageFragmentBinding.inflate(layoutInflater)

    override fun initData() {
        addMenuItem("亚洲人骨质疏松自我筛查工具") { TODO("Wait for Fragment") } // { OsteoporosisScreening_fm() }
        addMenuItem("Denis分型（爆裂骨折）") { TODO("Wait for Fragment") } // { DenisBurstFracture_fm() }
        addMenuItem("骨质疏松风险一分钟测试题") { TODO("Wait for Fragment") } // { OsteoporosisRiskTest_fm() }
        addMenuItem("Delee分型（尺骨鹰嘴骨折）") { TODO("Wait for Fragment") } // { DeleeFracture_fm() }
        addMenuItem("改良Evans分类（股骨转子间骨折）") { TODO("Wait for Fragment") } // { EvansFracture_fm() }
        addMenuItem("Amor诊断标准（脊柱关节病诊断标准）") { TODO("Wait for Fragment") } // { AmorCriteria_fm() }
        addMenuItem("AO分类（肱骨近端骨折）") { TODO("Wait for Fragment") } // { AoHumerusFracture_fm() }
        addMenuItem("Frankel功能分级/ASIA分级（脊髓损伤严重程度）") { TODO("Wait for Fragment") } // { FrankelAsiaGrading_fm() }
        addMenuItem("腰间盘突出症的症状和体征") { TODO("Wait for Fragment") } // { LumbarDiscHerniation_fm() }
        addMenuItem("Sanders分类（根骨骨折）") { TODO("Wait for Fragment") } // { SandersCalcaneal_fm() }
        addMenuItem("腕管综合征功能评定表") { TODO("Wait for Fragment") } // { CarpalTunnelScale_fm() }
        addMenuItem("Frankel脊髓损伤分级法") { TODO("Wait for Fragment") } // { FrankelSpinalInjury_fm() }
        addMenuItem("Schatzker分型（胫骨平台骨折）") { TODO("Wait for Fragment") } // { SchatzkerFracture_fm() }
        addMenuItem("Lauge-Hansen分类（踝关节骨折）") { TODO("Wait for Fragment") } // { LaugeHansenAnkle_fm() }
        addMenuItem("Denis分类（脊柱骨折）") { TODO("Wait for Fragment") } // { DenisSpineFracture_fm() }
        addMenuItem("Green分类法（第一掌骨基底部骨折）") { TODO("Wait for Fragment") } // { GreenMetacarpalFracture_fm() }
        addMenuItem("HSS评分（美国特种外科医院膝关节评分）") { TODO("Wait for Fragment") } // { HssKneeScore_fm() }
        addMenuItem("骨肿瘤TNM分期") { TODO("Wait for Fragment") } // { BoneTumorTNM_fm() }
        addMenuItem("Neer分类（肱骨近端骨折）") { TODO("Wait for Fragment") } // { NeerHumerusFracture_fm() }
        addMenuItem("脊髓型颈椎病JOA评分（17分法）") { TODO("Wait for Fragment") } // { JoaCsmScore_fm() }
        addMenuItem("Tile分型（骨盆骨折）") { TODO("Wait for Fragment") } // { TilePelvicFracture_fm() }
        addMenuItem("Weber-Cech分类（骨折不愈合和延迟愈合）") { TODO("Wait for Fragment") } // { WeberCechNonunion_fm() }
        addMenuItem("TLICS评分系统（胸腰椎损伤）") { TODO("Wait for Fragment") } // { TlicsScore_fm() }
        addMenuItem("腰背痛手术评分标准") { TODO("Wait for Fragment") } // { LowBackPainSurgeryScore_fm() }
        addMenuItem("颈神经根受累的症状和体征") { TODO("Wait for Fragment") } // { CervicalRadiculopathy_fm() }
        addMenuItem("Codman分类（肱骨近端骨折）") { TODO("Wait for Fragment") } // { CodmanHumerusFracture_fm() }
        addMenuItem("颈性眩晕症状与功能评估量表") { TODO("Wait for Fragment") } // { CervicalVertigoScale_fm() }
        addMenuItem("改良Ellis分类（胫骨骨折）") { TODO("Wait for Fragment") } // { EllisTibialFracture_fm() }
        addMenuItem("Anderson分类（开放性骨折）") { TODO("Wait for Fragment") } // { AndersonOpenFracture_fm() }
        addMenuItem("锁骨骨折分类") { TODO("Wait for Fragment") } // { ClavicleFracture_fm() }
        addMenuItem("肱骨髁上骨折分类") { TODO("Wait for Fragment") } // { SupracondylarHumerusFracture_fm() }
        addMenuItem("AO分型（髋臼骨折）") { TODO("Wait for Fragment") } // { AoAcetabularFracture_fm() }
        addMenuItem("颈椎病患者脊髓功能状态评定") { TODO("Wait for Fragment") } // { CervicalSpondylosisSpinalFunction_fm() }
        addMenuItem("肩关节后脱位分型") { TODO("Wait for Fragment") } // { ShoulderPosteriorDislocation_fm() }
        addMenuItem("JOA腰背痛疾病治疗成绩标准评分") { TODO("Wait for Fragment") } // { JoaLowBackPainScore_fm() }
        addMenuItem("Gustilo-Anderson分类（开放性骨折）") { TODO("Wait for Fragment") } // { GustiloAndersonOpenFracture_fm() }
        addMenuItem("Danis-Weber分类（髁关节骨折）") { TODO("Wait for Fragment") } // { DanisWeberFracture_fm() }
        addMenuItem("Bado分型（孟氏骨折Monteggia骨折）") { TODO("Wait for Fragment") } // { BadoMonteggiaFracture_fm() }
        addMenuItem("AO分型（舟骨骨折）") { TODO("Wait for Fragment") } // { AoScaphoidFracture_fm() }
        addMenuItem("Young/Burgress分型（骨盆骨折）") { TODO("Wait for Fragment") } // { YoungBurgressPelvicFracture_fm() }
        addMenuItem("HSS评分（肘关节）") { TODO("Wait for Fragment") } // { HssElbowScore_fm() }
        addMenuItem("Rockwood分类（复发性肩关节半脱位）") { TODO("Wait for Fragment") } // { RockwoodShoulderSubluxation_fm() }
        addMenuItem("Fernanda分类（桡骨远端骨折）") { TODO("Wait for Fragment") } // { FernandaFracture_fm() }
        addMenuItem("Smith骨折分类（桡骨远端骨折）") { TODO("Wait for Fragment") } // { SmithFracture_fm() }
        addMenuItem("肱骨外髁骨折分型（北京积水潭医院）") { TODO("Wait for Fragment") } // { HumerusLateralCondyleFracture_fm() }
        addMenuItem("Allman分类法（肩锁关节脱位）") { TODO("Wait for Fragment") } // { AllmanDislocation_fm() }
        addMenuItem("Fryman分类（桡骨远端骨折）") { TODO("Wait for Fragment") } // { FrymanFracture_fm() }
        addMenuItem("漏斗胸指数") { TODO("Wait for Fragment") } // { PectusExcavatumIndex_fm() }
        addMenuItem("Gartland分型（肱骨髁上骨折）") { TODO("Wait for Fragment") } // { GartlandFracture_fm() }
        addMenuItem("腕舟骨骨折") { TODO("Wait for Fragment") } // { ScaphoidFracture_fm() }
        addMenuItem("舟月不稳定分级") { TODO("Wait for Fragment") } // { ScapholunateInstability_fm() }
        addMenuItem("肱骨内上髁骨折分度") { TODO("Wait for Fragment") } // { HumerusMedialCondyleFracture_fm() }
        addMenuItem("Kocher分类（肱骨近端骨折）") { TODO("Wait for Fragment") } // { KocherHumerusFracture_fm() }
        addMenuItem("肱骨髁间骨折机制分型") { TODO("Wait for Fragment") } // { IntercondylarHumerusFracture_fm() }
        addMenuItem("朱通伯分类（开放性骨折）") { TODO("Wait for Fragment") } // { ZhuOpenFracture_fm() }
        addMenuItem("王亦璁分类（开放性骨折）") { TODO("Wait for Fragment") } // { WangOpenFracture_fm() }
        addMenuItem("Lindstrom分度（桡骨远端骨折）") { TODO("Wait for Fragment") } // { LindstromFracture_fm() }
        addMenuItem("Roland-Morris腰椎功能障碍问卷") { TODO("Wait for Fragment") } // { RolandMorrisQuestionnaire_fm() }
        addMenuItem("Hotchkiss分类（桡骨头骨折）") { TODO("Wait for Fragment") } // { HotchkissFracture_fm() }
        addMenuItem("Rockwood分类法（肩锁关节脱位）") { TODO("Wait for Fragment") } // { RockwoodAcDislocation_fm() }
        addMenuItem("Herbert分型（舟骨骨折）") { TODO("Wait for Fragment") } // { HerbertFracture_fm() }
        addMenuItem("Mason分类（桡骨头骨折）") { TODO("Wait for Fragment") } // { MasonFracture_fm() }
        addMenuItem("Neer分型（锁骨远端骨折）") { TODO("Wait for Fragment") } // { NeerClavicleFracture_fm() }
        addMenuItem("Cassebaum评分系统（肘关节）") { TODO("Wait for Fragment") } // { CassebaumScore_fm() }
        addMenuItem("Watson-Jones分类（肱骨近端骨折）") { TODO("Wait for Fragment") } // { WatsonJonesFracture_fm() }
        addMenuItem("Miller分类（肩胛骨骨折）") { TODO("Wait for Fragment") } // { MillerScapulaFracture_fm() }
        addMenuItem("Enneking外科分期（肌肉骨骼肿瘤）") { TODO("Wait for Fragment") } // { EnnekingStaging_fm() }
        addMenuItem("Ideberg分型（关节盂软骨内骨折）") { TODO("Wait for Fragment") } // { IdebergFracture_fm() }
        addMenuItem("Tossy分型（肩锁关节脱位分型）") { TODO("Wait for Fragment") } // { TossyDislocation_fm() }
        addMenuItem("Riseborough分型（肱骨髁间骨折）") { TODO("Wait for Fragment") } // { RiseboroughFracture_fm() }
        addMenuItem("Pavlov比值测定") { TODO("Wait for Fragment") } // { PavlovRatio_fm() }
        addMenuItem("Letournel-Judet分型（髋臼骨折）") { TODO("Wait for Fragment") } // { LetournelJudetFracture_fm() }
        addMenuItem("桡骨远端骨折按关节面情况的分级") { TODO("Wait for Fragment") } // { DistalRadiusFractureGrading_fm() }
        addMenuItem("肩关节疾患治疗成绩判定标准") { TODO("Wait for Fragment") } // { ShoulderTreatmentScore_fm() }
        addMenuItem("Regan分型（肘关节尺骨冠状突骨折）") { TODO("Wait for Fragment") } // { ReganFracture_fm() }
        addMenuItem("美国肩肘外科评分") { TODO("Wait for Fragment") } // { AsesScore_fm() }
        addMenuItem("Mehdian分型标准（肱骨小头骨折Kocher骨折）") { TODO("Wait for Fragment") } // { MehdianFracture_fm() }
        addMenuItem("Karlsson术后疗效评分标准") { TODO("Wait for Fragment") } // { KarlssonPostopScore_fm() }
        addMenuItem("Mcintyre肱骨髁上骨折分类表") { TODO("Wait for Fragment") } // { McintyreFracture_fm() }
        addMenuItem("Thomas分类（肩关节不稳定）") { TODO("Wait for Fragment") } // { ThomasShoulderInstability_fm() }
        addMenuItem("Ferguson分型（屈曲压缩骨折）") { TODO("Wait for Fragment") } // { FergusonFracture_fm() }
        addMenuItem("Silliman分类（肩关节不稳定）") { TODO("Wait for Fragment") } // { SillimanShoulderInstability_fm() }
        addMenuItem("Lazzcano术后疗效评分标准") { TODO("Wait for Fragment") } // { LazzcanoPostopScore_fm() }
        addMenuItem("月骨周围脱位分期") { TODO("Wait for Fragment") } // { PerilunateDislocationStaging_fm() }
        addMenuItem("Ishida-Ikuta评分系统（近指间关节骨折脱位）") { TODO("Wait for Fragment") } // { IshidaIkutaScore_fm() }
        addMenuItem("月骨周围脱位根据损伤程度与位置分型") { TODO("Wait for Fragment") } // { PerilunateDislocationType_fm() }
        addMenuItem("Zdravkovic-Damholt分型（肩胛骨）") { TODO("Wait for Fragment") } // { ZdravkovicDamholtScapula_fm() }
        addMenuItem("PSI分类（肩关节后方不稳定）") { TODO("Wait for Fragment") } // { PsiShoulderInstability_fm() }
        addMenuItem("Eyres-Brooks分型（肩胛骨喙突骨折）") { TODO("Wait for Fragment") } // { EyresBrooksScapula_fm() }
    }
}
